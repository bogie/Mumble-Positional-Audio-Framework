package mpaf.json;

import java.util.Collection;

public class ChannelListJson {
	@SuppressWarnings("unused")
	private Collection<ChannelJson> channels;

	public ChannelListJson(Collection<ChannelJson> channels) {
		this.channels = channels;
	}
}
